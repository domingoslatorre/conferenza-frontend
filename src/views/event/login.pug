extends ../includes/master-event

block vars
  - var activeEvent = event.pages.login.file

block header
  +event-header(event.pages.login)

block content
  div(class='container cf-main')
    .row
      .col
        h1.cf-title-1 #{event.pages.login.title}