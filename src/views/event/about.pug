extends ../includes/master-event

block vars
  - var activeEvent = event.pages.about.file

block header
  +event-header(event.pages.about)

block content
  div(class='container cf-main')
    .row
      .col
        h1.cf-title-1 #{event.pages.about.title}