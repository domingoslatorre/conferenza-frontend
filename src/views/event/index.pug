extends ../includes/master-event

block vars
  - var activeEvent = event.pages.editions.file

block header
  +event-header(event.pages.editions)

block content
  div(class='container cf-main')
    .row
      .col
        h1.cf-title-1 #{event.pages.editions.title}